[project]
name = "supervisor"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "langchain-ollama",
    "langgraph",
    "aiosqlite>=0.20.0",
    "langgraph-checkpoint-sqlite>=2.0.0",
]
